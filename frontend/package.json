{
  "name": "e-ward-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:publish": "vite build && node -e \"require('fs').cpSync('dist', '../public', {recursive: true})\"",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
